[project]
name = "ASCII Art Converter"
version = "0.1.0"
description = "The Ascii art convertion of image and video run on your terminal."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opencv-python>=4.13.0.92",
]
